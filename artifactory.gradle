artifactory {
    contextUrl = "http://server.bbkr.space:8081/artifactory/"
    publish {
        repository {
            /*if (project.version.toString().contains("SNAPSHOT")) {
                repoKey = "libs-snapshot-local"
            } else {
                repoKey = "libs-release-local"
            }*/

            repoKey = "libs-release-local"
            if (project.hasProperty("artifactoryUser")) {
                username = project.artifactoryUser
                password = project.artifactoryPassword
            } else {
                println("Artifactory not configured! Set artifactoryUser and artifactoryPassword in private.gradle before running artifactoryPublish.")
            }
        }
        defaults {
            publications("maven")
        }
    }
}
